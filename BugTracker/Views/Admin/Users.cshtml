@{
    ViewBag.Title = "Users";
}
@using BugTracker.Helpers;
@model List <BugTracker.Models.ApplicationUser>

<div class="container">
    <h2> Users Index </h2>
   
        @*<p>
            @Html.ActionLink("Create New", "Create")
        </p>*@
   
    <div class="row">
        <div class="col-md-10">
            <div class="panel panel-green margin-bottom-40">
                <div class="panel-heading">
                    <h3 class="panel-title"> Users</h3>
                </div>
                <table class="table table-hover">
                    <thead>
                        <tr>
                            <th>User Name</th>
                            <th>Email</th>
                            <th>Display Name</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var user in Model)
                        {
                            <tr>
                                <td>@user.UserName   </td>
                                <td>@user.Email   </td>
                                <td>@user.DisplayName   </td>
                                <td><a href="@Url.Action("EditUser", new { Id = user.Id})">   Edit</a></td>
                                <td><a href="@Url.Action("DetailsUserRoles", new { Id = user.Id})">  Details</a></td>
                                
                                @if (user.IsUserInRole("Developer"))
                                {
                                    <td><a href="@Url.Action("ListTickets","Projects", new { assignedId = user.Id})">  Tickets Assigned</a></td>
                                }
                                <td><a href="@Url.Action("ListTickets","Projects", new { ownerId = user.Id})">  Tickets Owned</a></td>
                                
                            </tr>
                        }

                    </tbody>
                </table>
            </div>
        </div>
        <!--End Hover Rows-->
    </div>
</div>


